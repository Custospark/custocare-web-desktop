import React, { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { motion, AnimatePresence, useScroll, useTransform } from 'framer-motion';
import { 
  Heart, 
  Shield, 
  Stethoscope, 
  ChevronRight,
  Sparkles,
  CheckCircle2,
  Users,
  Lock,
  Sun,
  Moon,
  Building,
  UserCheck,
  Activity,
  Download,
  Monitor,
  ArrowRight,
  TrendingUp,
  Clock,
  Award,
  Zap,
  Brain,
  Database,
  Fingerprint
} from 'lucide-react';
import { useAppSelector, useAppDispatch } from '../../../../app/store/hooks/useApp';
import { toggleTheme } from '../../../../app/store/slices/uiSlice';
import { cn } from '../../../../shared/types/cn';

/* ==========================================================================
   ENHANCED PROFESSIONAL HEALTHCARE IMAGES
   ========================================================================== */
const IMAGES = {
  heroMedical: 'https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?w=1200&h=800&fit=crop&q=80',
  doctorPatient: 'https://images.unsplash.com/photo-1612349317150-e413f6a5b16d?w=600&h=400&fit=crop&q=80',
  hospitalFacility: 'https://images.unsplash.com/photo-1519494140681-8b17d830a3e9?w=600&h=400&fit=crop&q=80',
  medicalDashboard: 'https://images.unsplash.com/photo-1579684385127-1ef15d508118?w=600&h=400&fit=crop&q=80',
  aiHealthcare: 'https://images.unsplash.com/photo-1559757175-0eb30cd8c063?w=600&h=400&fit=crop&q=80'
};

/* ==========================================================================
   PREMIUM FRAMER MOTION VARIANTS
   ========================================================================== */
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.15,
      delayChildren: 0.3
    }
  }
};

const fadeInUp = {
  hidden: { opacity: 0, y: 30 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      type: "spring",
      stiffness: 120,
      damping: 18,
      mass: 0.8
    }
  }
};

const scaleIn = {
  hidden: { opacity: 0, scale: 0.95 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: {
      type: "spring",
      stiffness: 150,
      damping: 20
    }
  }
};

const cardHoverVariants = {
  rest: { 
    scale: 1, 
    y: 0,
    boxShadow: "0 4px 24px rgba(0, 0, 0, 0.08)"
  },
  hover: {
    scale: 1.03,
    y: -8,
    boxShadow: "0 20px 48px rgba(0, 0, 0, 0.15)",
    transition: {
      type: "spring",
      stiffness: 400,
      damping: 25
    }
  }
};

const floatAnimation = {
  animate: {
    y: [-12, 12, -12],
    transition: {
      duration: 8,
      repeat: Infinity,
      ease: "easeInOut"
    }
  }
};

const pulseGlow = {
  animate: {
    scale: [1, 1.08, 1],
    opacity: [0.4, 0.7, 0.4],
    transition: {
      duration: 4,
      repeat: Infinity,
      ease: "easeInOut"
    }
  }
};

/* ==========================================================================
   ACCESSIBILITY & SCREEN READER UTILITIES
   ========================================================================== */
const A11y = {
  srOnly: "sr-only",
  focusStyles: "focus:outline-none focus:ring-3 focus:ring-blue-500/50 focus:ring-offset-2 dark:focus:ring-offset-gray-900",
  buttonLabel: (action: string) => `${action} - CustoCare AI Healthcare Platform`,
  imageAlt: (context: string) => `${context} - Premium healthcare technology visualization`
};

/* ==========================================================================
   TYPOGRAPHY SYSTEM
   ========================================================================== */
const Typography = {
  h1: "font-serif font-bold tracking-tight leading-[1.1]",
  h2: "font-serif font-semibold tracking-tight leading-tight",
  h3: "font-sans font-bold tracking-normal",
  body: "font-sans font-normal leading-relaxed",
  caption: "font-sans text-xs tracking-wide uppercase"
};

export const Landing: React.FC = () => {
  const navigate = useNavigate();
  const dispatch = useAppDispatch();
  const theme = useAppSelector((state) => state.ui.theme);
  const [isLoading, setIsLoading] = useState<string | null>(null);
  const [hoveredOption, setHoveredOption] = useState<string | null>(null);
  const { scrollYProgress } = useScroll();
  const headerOpacity = useTransform(scrollYProgress, [0, 0.2], [1, 0.9]);
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    const checkMobile = () => setIsMobile(window.innerWidth < 1024);
    checkMobile();
    window.addEventListener('resize', checkMobile);
    return () => window.removeEventListener('resize', checkMobile);
  }, []);

  const handleAction = async (action: 'login' | 'signup') => {
    setIsLoading(action);
    
    try {
      // Premium loading experience with staggered animations
      await Promise.all([
        new Promise(resolve => setTimeout(resolve, 800)),
        new Promise(resolve => setTimeout(resolve, 1200))
      ]);
      
      if (action === 'login') {
        navigate('/login');
      } else {
        navigate('/signup');
      }
    } catch (error) {
      setIsLoading(null);
    }
  };

  const handleDownloadWindows = () => {
    // Tooltip feedback for download action
    const tooltip = document.createElement('div');
    tooltip.className = cn(
      "fixed z-[9999] px-4 py-2 rounded-lg font-medium text-sm shadow-xl",
      theme === 'dark' 
        ? "bg-gray-800 text-white border border-gray-700" 
        : "bg-white text-gray-900 border border-gray-200"
    );
    tooltip.textContent = 'Downloading CustoCare AI for Windows...';
    tooltip.style.top = '20px';
    tooltip.style.right = '20px';
    document.body.appendChild(tooltip);
    
    setTimeout(() => {
      tooltip.remove();
      window.location.href = '/downloads/custocare-windows.exe';
    }, 1500);
  };

  /* ==========================================================================
     TRUST BADGES COMPONENT
     ========================================================================== */
  const TrustBadges = () => (
    <motion.div 
      variants={containerVariants}
      initial="hidden"
      animate="visible"
      role="region"
      aria-label="Security Certifications"
      className="flex flex-wrap items-center justify-center lg:justify-start gap-3 mb-8"
    >
      {[
        { 
          icon: Shield, 
          text: "HIPAA Compliant", 
          description: "Healthcare data protection certified",
          color: "emerald" 
        },
        { 
          icon: Lock, 
          text: "256-bit Encryption", 
          description: "Military-grade security",
          color: "blue" 
        },
        { 
          icon: Award, 
          text: "ISO 27001 Certified", 
          description: "Enterprise security standard",
          color: "purple" 
        },
        { 
          icon: Fingerprint, 
          text: "GDPR Ready", 
          description: "Global data privacy compliance",
          color: "amber" 
        }
      ].map((badge, index) => (
        <motion.div
          key={index}
          variants={fadeInUp}
          whileHover={{ scale: 1.05, y: -2 }}
          whileTap={{ scale: 0.98 }}
          role="listitem"
          className={cn(
            "group relative flex items-center gap-3 px-4 py-3 rounded-xl backdrop-blur-sm border transition-all duration-300 cursor-help",
            badge.color === 'emerald' && "bg-emerald-500/5 border-emerald-500/15 hover:bg-emerald-500/10",
            badge.color === 'blue' && "bg-blue-500/5 border-blue-500/15 hover:bg-blue-500/10",
            badge.color === 'purple' && "bg-purple-500/5 border-purple-500/15 hover:bg-purple-500/10",
            badge.color === 'amber' && "bg-amber-500/5 border-amber-500/15 hover:bg-amber-500/10"
          )}
        >
          {/* Tooltip for accessibility */}
          <div className={cn(
            "absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-3 py-2 rounded-lg text-xs font-medium opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap shadow-lg z-50",
            theme === 'dark' ? "bg-gray-800 text-gray-200" : "bg-gray-900 text-gray-100"
          )}>
            {badge.description}
            <div className="absolute top-full left-1/2 transform -translate-x-1/2 -mt-1 w-2 h-2 rotate-45 bg-inherit" />
          </div>

          <badge.icon className={cn(
            "w-4 h-4 flex-shrink-0",
            badge.color === 'emerald' && "text-emerald-600 dark:text-emerald-400",
            badge.color === 'blue' && "text-blue-600 dark:text-blue-400",
            badge.color === 'purple' && "text-purple-600 dark:text-purple-400",
            badge.color === 'amber' && "text-amber-600 dark:text-amber-400"
          )} 
          aria-hidden="true"
          />
          <span className={cn(
            "text-sm font-semibold",
            badge.color === 'emerald' && "text-emerald-700 dark:text-emerald-300",
            badge.color === 'blue' && "text-blue-700 dark:text-blue-300",
            badge.color === 'purple' && "text-purple-700 dark:text-purple-300",
            badge.color === 'amber' && "text-amber-700 dark:text-amber-300"
          )}>
            {badge.text}
          </span>
        </motion.div>
      ))}
    </motion.div>
  );

  return (
    <div className={cn(
      "min-h-screen flex flex-col overflow-hidden relative antialiased",
      theme === 'dark' 
        ? "bg-gradient-to-br from-gray-950 via-gray-900 to-gray-950 text-gray-100" 
        : "bg-gradient-to-br from-gray-25 via-white to-blue-50/30 text-gray-900"
    )}>
      {/* Enhanced Premium Background Effects */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        {/* Animated gradient orbs */}
        <motion.div 
          variants={pulseGlow}
          animate="animate"
          className={cn(
            "absolute -top-40 -right-40 w-[800px] h-[800px] rounded-full blur-3xl",
            theme === 'dark' ? "bg-blue-600/15" : "bg-blue-400/10"
          )}
        />
        <motion.div 
          variants={pulseGlow}
          animate="animate"
          style={{ animationDelay: '2s' }}
          className={cn(
            "absolute -bottom-40 -left-40 w-[800px] h-[800px] rounded-full blur-3xl",
            theme === 'dark' ? "bg-emerald-600/15" : "bg-emerald-400/10"
          )}
        />
        
        {/* Subtle grid pattern */}
        <div className={cn(
          "absolute inset-0 opacity-[0.015]",
          theme === 'dark' ? "bg-grid-white/5" : "bg-grid-gray-900/5"
        )} 
        style={{
          backgroundSize: '80px 80px',
          backgroundImage: `linear-gradient(to right, currentColor 1px, transparent 1px), linear-gradient(to bottom, currentColor 1px, transparent 1px)`
        }}
        />
      </div>

      {/* Premium Enterprise Header */}
      <motion.header 
        style={{ opacity: headerOpacity }}
        className={cn(
          "sticky top-0 z-50 border-b backdrop-blur-2xl transition-all duration-300 supports-[backdrop-filter]:bg-opacity-70",
          theme === 'dark' 
            ? "bg-gray-900/80 border-gray-800/60" 
            : "bg-white/80 border-gray-200/60"
        )}
        role="banner"
      >
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex items-center justify-between">
            {/* Premium Logo */}
            <motion.div 
              initial={{ opacity: 0, x: -20 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6, ease: "easeOut" }}
              className="flex items-center gap-3"
            >
              <motion.div 
                whileHover={{ rotate: 360, scale: 1.1 }}
                whileTap={{ scale: 0.95 }}
                transition={{ duration: 0.6, type: "spring" }}
                className={cn(
                  "w-12 h-12 rounded-2xl flex items-center justify-center shadow-xl",
                  "bg-gradient-to-br from-blue-600 via-blue-500 to-emerald-600",
                  "hover:shadow-blue-500/25 hover:shadow-2xl"
                )}
                role="img"
                aria-label="CustoCare AI Heart Logo"
              >
                <Heart className="w-6 h-6 text-white" aria-hidden="true" />
              </motion.div>
              <div className="hidden sm:block">
                <div className={cn(
                  "text-2xl font-bold bg-gradient-to-r from-blue-600 to-emerald-600 bg-clip-text text-transparent",
                  Typography.h1
                )}>
                  CustoCare AI
                </div>
                <div className={cn(
                  "text-xs tracking-wider uppercase font-semibold",
                  theme === 'dark' ? "text-gray-400" : "text-gray-600",
                  Typography.caption
                )}>
                  Enterprise Healthcare Intelligence
                </div>
              </div>
            </motion.div>

            {/* Premium Action Buttons */}
            <motion.div 
              initial={{ opacity: 0, x: 20 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6, ease: "easeOut", delay: 0.1 }}
              className="flex items-center gap-2 sm:gap-3"
            >
              {/* Windows Download with Tooltip */}
              <motion.button
                whileHover={{ scale: 1.05, y: -1 }}
                whileTap={{ scale: 0.95 }}
                onClick={handleDownloadWindows}
                className={cn(
                  "group relative flex items-center gap-2.5 px-4 py-2.5 rounded-xl font-semibold text-sm transition-all duration-300",
                  A11y.focusStyles,
                  theme === 'dark'
                    ? "bg-gray-800/60 border border-gray-700/50 text-gray-300 hover:bg-gray-800 hover:border-blue-500/50 hover:text-white"
                    : "bg-white/90 border border-gray-300/80 text-gray-700 hover:bg-white hover:border-blue-500/50 hover:text-gray-900 shadow-lg hover:shadow-xl"
                )}
                aria-label="Download for Windows - Enterprise Healthcare Platform"
              >
                <Monitor className="w-4 h-4" aria-hidden="true" />
                <span className="hidden sm:inline">Windows App</span>
                <Download className="w-4 h-4" aria-hidden="true" />
                
                {/* Tooltip */}
                <div className={cn(
                  "absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-3 py-2 rounded-lg text-xs font-medium opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap shadow-lg z-50",
                  theme === 'dark' ? "bg-gray-800 text-gray-200" : "bg-gray-900 text-gray-100"
                )}>
                  Download Enterprise Edition
                  <div className="absolute top-full left-1/2 transform -translate-x-1/2 -mt-1 w-2 h-2 rotate-45 bg-inherit" />
                </div>
              </motion.button>

              {/* Theme Toggle with Animation */}
              <motion.button
                whileHover={{ scale: 1.05, rotate: 180 }}
                whileTap={{ scale: 0.95 }}
                onClick={() => dispatch(toggleTheme())}
                className={cn(
                  "p-2.5 rounded-xl transition-all duration-300 border",
                  A11y.focusStyles,
                  theme === 'dark'
                    ? "bg-gray-800/60 hover:bg-gray-700 text-gray-300 border-gray-700/50"
                    : "bg-white/90 hover:bg-gray-50 text-gray-600 border-gray-300/80 shadow-lg"
                )}
                aria-label={theme === 'dark' ? 'Switch to light theme' : 'Switch to dark theme'}
              >
                <AnimatePresence mode="wait">
                  {theme === 'dark' ? (
                    <motion.div
                      key="sun"
                      initial={{ rotate: -90, opacity: 0 }}
                      animate={{ rotate: 0, opacity: 1 }}
                      exit={{ rotate: 90, opacity: 0 }}
                      transition={{ duration: 0.3, ease: "easeInOut" }}
                    >
                      <Sun className="w-5 h-5" aria-hidden="true" />
                    </motion.div>
                  ) : (
                    <motion.div
                      key="moon"
                      initial={{ rotate: 90, opacity: 0 }}
                      animate={{ rotate: 0, opacity: 1 }}
                      exit={{ rotate: -90, opacity: 0 }}
                      transition={{ duration: 0.3, ease: "easeInOut" }}
                    >
                      <Moon className="w-5 h-5" aria-hidden="true" />
                    </motion.div>
                  )}
                </AnimatePresence>
              </motion.button>
            </motion.div>
          </div>
        </div>
      </motion.header>

      {/* Main Content Area */}
      <main className="flex-1 flex flex-col items-center justify-center px-4 py-8 sm:py-12 lg:py-16 relative z-10">
        <div className="w-full max-w-7xl">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-16 items-center">
            
            {/* MOBILE: Access Panel First (Optimized for mobile) */}
            {isMobile && (
              <motion.div 
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, ease: "easeOut" }}
                className="w-full max-w-md mx-auto"
              >
                <AccessPanel 
                  theme={theme}
                  isLoading={isLoading}
                  hoveredOption={hoveredOption}
                  setHoveredOption={setHoveredOption}
                  handleAction={handleAction}
                  isMobile={isMobile}
                />
              </motion.div>
            )}

            {/* Left Column - Premium Hero Content */}
            <motion.div 
              variants={containerVariants}
              initial="hidden"
              animate="visible"
              className="text-center lg:text-left space-y-8 lg:space-y-12"
            >
              <div className="space-y-8">
                {/* Premium Badge */}
                <motion.div variants={fadeInUp}>
                  <motion.div 
                    whileHover={{ scale: 1.05, y: -2 }}
                    className={cn(
                      "inline-flex items-center gap-2.5 px-4 py-2.5 rounded-full shadow-lg",
                      "bg-gradient-to-r from-blue-600/10 to-emerald-600/10",
                      theme === 'dark' 
                        ? "border border-blue-500/20 text-blue-400" 
                        : "border border-blue-500/30 text-blue-600"
                    )}
                  >
                    <Sparkles className="w-4 h-4 animate-pulse" aria-hidden="true" />
                    <span className={cn(
                      "text-sm font-bold tracking-wide",
                      Typography.caption
                    )}>
                      Trusted by 10,000+ Healthcare Institutions
                    </span>
                  </motion.div>
                </motion.div>
                
                {/* Hero Headline */}
                <div className="space-y-6">
                  <motion.h1 
                    variants={fadeInUp}
                    className={cn(
                      "text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-bold leading-[1.05] tracking-tight",
                      Typography.h1,
                      theme === 'dark' ? "text-white" : "text-gray-900"
                    )}
                  >
                    Healthcare Intelligence,{' '}
                    <span className="relative inline-block">
                      <span className="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-emerald-600 to-blue-600 animate-gradient bg-[length:200%_auto]">
                        Personalized Care.
                      </span>
                      <motion.div
                        className="absolute -bottom-3 left-0 right-0 h-1 bg-gradient-to-r from-blue-600 to-emerald-600 rounded-full"
                        initial={{ scaleX: 0 }}
                        animate={{ scaleX: 1 }}
                        transition={{ duration: 1, delay: 0.5, ease: "easeOut" }}
                      />
                    </span>
                  </motion.h1>
                  
                  <motion.p 
                    variants={fadeInUp}
                    className={cn(
                      "text-xl sm:text-2xl lg:text-3xl font-light leading-relaxed max-w-3xl mx-auto lg:mx-0",
                      Typography.body,
                      theme === 'dark' ? "text-gray-300" : "text-gray-700"
                    )}
                  >
                    Transforming healthcare through AI-powered intelligence 
                    and compassionate care delivery.
                  </motion.p>
                </div>
              </div>

              {/* Premium Benefits Grid */}
              <motion.div 
                variants={containerVariants}
                className="grid grid-cols-1 sm:grid-cols-3 gap-4 sm:gap-6"
                role="region"
                aria-label="Platform Benefits"
              >
                {[
                  {
                    icon: UserCheck,
                    title: "For Patients",
                    description: "Personal health monitoring & AI-powered insights",
                    gradient: "from-blue-500 to-blue-600",
                    features: ["Health Tracking", "AI Insights", "24/7 Support"]
                  },
                  {
                    icon: Stethoscope,
                    title: "For Medical Professionals",
                    description: "Clinical decision support & patient management",
                    gradient: "from-emerald-500 to-emerald-600",
                    features: ["Decision Support", "EHR Integration", "Analytics"]
                  },
                  {
                    icon: Building,
                    title: "For Facilities",
                    description: "Operational efficiency & comprehensive analytics",
                    gradient: "from-purple-500 to-purple-600",
                    features: ["Operations", "Analytics", "Compliance"]
                  }
                ].map((benefit, index) => (
                  <motion.div
                    key={index}
                    variants={scaleIn}
                    whileHover="hover"
                    initial="rest"
                    animate="rest"
                    role="article"
                    aria-label={`${benefit.title} Benefits`}
                    className={cn(
                      "group relative p-6 rounded-2xl border transition-all duration-300 cursor-pointer overflow-hidden",
                      theme === 'dark'
                        ? "bg-gray-800/40 border-gray-700/50 hover:bg-gray-800/60 hover:border-gray-600"
                        : "bg-white/80 border-gray-200/80 hover:bg-white hover:border-gray-300 shadow-lg hover:shadow-2xl"
                    )}
                  >
                    {/* Hover gradient overlay */}
                    <div className="absolute inset-0 bg-gradient-to-br from-transparent via-transparent to-transparent group-hover:via-blue-500/5 group-hover:to-purple-500/5 transition-all duration-500" />
                    
                    <motion.div 
                      variants={cardHoverVariants}
                      className={cn(
                        "relative inline-flex items-center justify-center w-14 h-14 rounded-2xl mb-5 shadow-xl",
                        `bg-gradient-to-br ${benefit.gradient}`
                      )}
                    >
                      <benefit.icon className="w-7 h-7 text-white" aria-hidden="true" />
                    </motion.div>
                    
                    <h3 className={cn(
                      "font-bold mb-3 text-lg group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors duration-300",
                      Typography.h3,
                      theme === 'dark' ? "text-white" : "text-gray-900"
                    )}>
                      {benefit.title}
                    </h3>
                    
                    <p className={cn(
                      "text-sm leading-relaxed mb-4",
                      Typography.body,
                      theme === 'dark' ? "text-gray-400" : "text-gray-600"
                    )}>
                      {benefit.description}
                    </p>
                    
                    <div className="flex flex-wrap gap-2">
                      {benefit.features.map((feature, i) => (
                        <span
                          key={i}
                          className={cn(
                            "px-3 py-1.5 text-xs rounded-full font-medium",
                            theme === 'dark' 
                              ? "bg-gray-800/60 text-gray-300" 
                              : "bg-gray-100 text-gray-700"
                          )}
                        >
                          {feature}
                        </span>
                      ))}
                    </div>
                  </motion.div>
                ))}
              </motion.div>

              {/* Trust & Security */}
              <motion.div 
                variants={fadeInUp}
                role="region"
                aria-label="Trust and Security"
              >
                <TrustBadges />
              </motion.div>

              {/* Social Proof - Enhanced */}
              <motion.div 
                variants={fadeInUp}
                className="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-8 pt-4"
                role="region"
                aria-label="User Statistics"
              >
                <div className="flex items-center gap-4">
                  <div className="relative">
                    <div className="flex -space-x-3">
                      {[1, 2, 3, 4, 5].map((i) => (
                        <motion.div
                          key={i}
                          initial={{ opacity: 0, scale: 0 }}
                          animate={{ opacity: 1, scale: 1 }}
                          transition={{ delay: 0.8 + i * 0.1, type: "spring" }}
                          className={cn(
                            "w-11 h-11 rounded-full border-3 shadow-lg",
                            theme === 'dark' 
                              ? "border-gray-900 bg-gradient-to-br from-blue-400 to-emerald-400" 
                              : "border-white bg-gradient-to-br from-blue-500 to-emerald-500"
                          )}
                        />
                      ))}
                    </div>
                    <motion.div
                      animate={{ rotate: 360 }}
                      transition={{ duration: 20, repeat: Infinity, ease: "linear" }}
                      className="absolute -inset-2 rounded-full border-2 border-dashed border-blue-400/30"
                    />
                  </div>
                  <div className="text-left">
                    <div className={cn(
                      "text-lg font-bold mb-1",
                      Typography.h3,
                      theme === 'dark' ? "text-white" : "text-gray-900"
                    )}>
                      50,000+ Active Users
                    </div>
                    <div className="flex items-center gap-2">
                      <div className="flex items-center gap-1">
                        {[...Array(5)].map((_, i) => (
                          <motion.div
                            key={i}
                            initial={{ opacity: 0, scale: 0 }}
                            animate={{ opacity: 1, scale: 1 }}
                            transition={{ delay: 1.2 + i * 0.05 }}
                          >
                            <CheckCircle2 className="w-4 h-4 text-emerald-500" aria-hidden="true" />
                          </motion.div>
                        ))}
                      </div>
                      <span className={cn(
                        "text-sm font-semibold",
                        theme === 'dark' ? "text-gray-400" : "text-gray-600"
                      )}>
                        4.9/5 • Expert Rated
                      </span>
                    </div>
                  </div>
                </div>
                
                <div className={cn(
                  "h-12 w-px hidden sm:block",
                  theme === 'dark' ? "bg-gray-700" : "bg-gray-300"
                )} />
                
                <div className="text-center sm:text-left">
                  <div className={cn(
                    "text-2xl font-bold mb-1",
                    Typography.h2,
                    theme === 'dark' ? "text-white" : "text-gray-900"
                  )}>
                    99.9%
                  </div>
                  <div className={cn(
                    "text-sm",
                    theme === 'dark' ? "text-gray-400" : "text-gray-600"
                  )}>
                    Platform Uptime
                  </div>
                </div>
              </motion.div>
            </motion.div>

            {/* DESKTOP: Premium Access Panel */}
            {!isMobile && (
              <motion.div 
                initial={{ opacity: 0, x: 30 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ duration: 0.7, delay: 0.4, ease: "easeOut" }}
                className="w-full max-w-md mx-auto"
              >
                <AccessPanel 
                  theme={theme}
                  isLoading={isLoading}
                  hoveredOption={hoveredOption}
                  setHoveredOption={setHoveredOption}
                  handleAction={handleAction}
                  isMobile={isMobile}
                />
              </motion.div>
            )}
          </div>

          {/* Enhanced Enterprise Stats Section */}
          <motion.div
            initial={{ opacity: 0, y: 40 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.6, ease: "easeOut" }}
            className={cn(
              "mt-16 lg:mt-24 p-8 lg:p-12 rounded-3xl border backdrop-blur-xl",
              theme === 'dark'
                ? "bg-gradient-to-br from-gray-800/40 to-gray-900/40 border-gray-700/50"
                : "bg-gradient-to-br from-white/70 to-blue-50/30 border-gray-200/50 shadow-2xl"
            )}
            role="region"
            aria-label="Platform Statistics"
          >
            <div className="text-center mb-12">
              <motion.div 
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.8 }}
                className="inline-flex items-center gap-3 mb-4"
              >
                <div className={cn(
                  "w-2 h-8 rounded-full",
                  "bg-gradient-to-b from-blue-600 to-emerald-600"
                )} />
                <h2 className={cn(
                  "text-2xl lg:text-3xl font-bold",
                  Typography.h2,
                  theme === 'dark' ? "text-white" : "text-gray-900"
                )}>
                  Measurable Healthcare Impact
                </h2>
                <div className={cn(
                  "w-2 h-8 rounded-full",
                  "bg-gradient-to-b from-emerald-600 to-blue-600"
                )} />
              </motion.div>
              <p className={cn(
                "text-lg max-w-2xl mx-auto",
                theme === 'dark' ? "text-gray-400" : "text-gray-600"
              )}>
                Delivering tangible results across the healthcare ecosystem
              </p>
            </div>
            
            <div className="grid grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
              {[
                { 
                  value: "50K+", 
                  label: "Active Patients", 
                  sublabel: "Growing daily", 
                  icon: Users, 
                  color: "blue",
                  trend: "+12% MoM"
                },
                { 
                  value: "98.7%", 
                  label: "Satisfaction Rate", 
                  sublabel: "Verified reviews", 
                  icon: Heart, 
                  color: "emerald",
                  trend: "Industry leading"
                },
                { 
                  value: "24/7", 
                  label: "AI Monitoring", 
                  sublabel: "Real-time alerts", 
                  icon: Activity, 
                  color: "purple",
                  trend: "Zero downtime"
                },
                { 
                  value: "47%", 
                  label: "Efficiency Boost", 
                  sublabel: "Average improvement", 
                  icon: TrendingUp, 
                  color: "orange",
                  trend: "Across facilities"
                }
              ].map((stat, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, scale: 0.8 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ delay: 0.9 + index * 0.1, type: "spring" }}
                  whileHover={{ y: -8, scale: 1.05 }}
                  className="text-center group cursor-pointer"
                  role="article"
                  aria-label={`${stat.label}: ${stat.value}`}
                >
                  <motion.div 
                    whileHover={{ rotate: 360, scale: 1.1 }}
                    transition={{ duration: 0.6, type: "spring" }}
                    className={cn(
                      "relative inline-flex items-center justify-center w-20 h-20 rounded-3xl mb-6 shadow-2xl",
                      `bg-gradient-to-br ${stat.color === 'blue' && 'from-blue-500 to-blue-600'} ${stat.color === 'emerald' && 'from-emerald-500 to-emerald-600'} ${stat.color === 'purple' && 'from-purple-500 to-purple-600'} ${stat.color === 'orange' && 'from-orange-500 to-orange-600'}`
                    )}
                  >
                    <stat.icon className="w-10 h-10 text-white" aria-hidden="true" />
                    <motion.div 
                      className="absolute -inset-4 rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"
                      style={{
                        background: `radial-gradient(circle, ${stat.color === 'blue' ? 'rgba(59, 130, 246, 0.2)' : stat.color === 'emerald' ? 'rgba(16, 185, 129, 0.2)' : stat.color === 'purple' ? 'rgba(168, 85, 247, 0.2)' : 'rgba(249, 115, 22, 0.2)'} 0%, transparent 70%)`
                      }}
                    />
                  </motion.div>
                  
                  <motion.div 
                    className={cn(
                      "text-4xl lg:text-5xl font-bold mb-3 group-hover:scale-105 transition-transform",
                      theme === 'dark' ? "text-white" : "text-gray-900",
                      Typography.h1
                    )}
                  >
                    {stat.value}
                  </motion.div>
                  
                  <div className={cn(
                    "text-lg font-semibold mb-2",
                    theme === 'dark' ? "text-gray-300" : "text-gray-700",
                    Typography.h3
                  )}>
                    {stat.label}
                  </div>
                  
                  <div className={cn(
                    "text-sm mb-2",
                    theme === 'dark' ? "text-gray-500" : "text-gray-500"
                  )}>
                    {stat.sublabel}
                  </div>
                  
                  <div className={cn(
                    "text-xs font-semibold px-3 py-1.5 rounded-full inline-block",
                    theme === 'dark' ? "bg-gray-800 text-gray-300" : "bg-gray-100 text-gray-700"
                  )}>
                    {stat.trend}
                  </div>
                </motion.div>
              ))}
            </div>
          </motion.div>
        </div>
      </main>

      {/* Premium Enterprise Footer */}
      <motion.footer 
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 1.2, ease: "easeOut" }}
        className={cn(
          "py-12 px-4 border-t backdrop-blur-2xl mt-24",
          theme === 'dark' 
            ? "bg-gray-900/70 border-gray-800/60" 
            : "bg-white/70 border-gray-200/60"
        )}
        role="contentinfo"
      >
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col lg:flex-row items-start justify-between gap-8 mb-10">
            {/* Brand & Description */}
            <div className="max-w-md">
              <div className="flex items-center gap-4 mb-6">
                <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-600 to-emerald-600 flex items-center justify-center shadow-lg">
                  <Heart className="w-7 h-7 text-white" aria-hidden="true" />
                </div>
                <div>
                  <div className={cn(
                    "text-2xl font-bold bg-gradient-to-r from-blue-600 to-emerald-600 bg-clip-text text-transparent",
                    Typography.h2
                  )}>
                    CustoCare AI
                  </div>
                  <div className={cn(
                    "text-sm font-semibold tracking-wider uppercase",
                    theme === 'dark' ? "text-gray-400" : "text-gray-600",
                    Typography.caption
                  )}>
                    Healthcare Intelligence Platform
                  </div>
                </div>
              </div>
              <p className={cn(
                "text-base leading-relaxed",
                theme === 'dark' ? "text-gray-400" : "text-gray-600",
                Typography.body
              )}>
                Delivering measurable healthcare outcomes through 
                AI-powered intelligence and compassionate innovation.
                Enterprise-grade security meets clinical excellence.
              </p>
            </div>
            
            {/* Quick Links */}
            <nav className="grid grid-cols-2 sm:grid-cols-4 gap-8" aria-label="Footer Navigation">
              {[
                { title: "Product", links: ["Features", "Security", "Pricing", "API"] },
                { title: "Resources", links: ["Documentation", "Case Studies", "Research", "Blog"] },
                { title: "Company", links: ["About", "Careers", "Press", "Partners"] },
                { title: "Legal", links: ["Privacy Policy", "Terms of Service", "HIPAA Compliance", "Contact"] }
              ].map((section) => (
                <div key={section.title}>
                  <h3 className={cn(
                    "font-bold text-lg mb-4",
                    theme === 'dark' ? "text-white" : "text-gray-900",
                    Typography.h3
                  )}>
                    {section.title}
                  </h3>
                  <ul className="space-y-3">
                    {section.links.map((link) => (
                      <li key={link}>
                        <motion.a
                          whileHover={{ x: 4 }}
                          href="#"
                          className={cn(
                            "text-sm transition-colors duration-300 hover:text-blue-600 dark:hover:text-blue-400",
                            theme === 'dark' ? "text-gray-400" : "text-gray-600",
                            A11y.focusStyles
                          )}
                          aria-label={`Navigate to ${link}`}
                        >
                          {link}
                        </motion.a>
                      </li>
                    ))}
                  </ul>
                </div>
              ))}
            </nav>
          </div>
          
          {/* Bottom Bar */}
          <div className={cn(
            "pt-8 border-t flex flex-col sm:flex-row items-center justify-between gap-4 text-sm",
            theme === 'dark' ? "border-gray-800 text-gray-500" : "border-gray-200 text-gray-500"
          )}>
            <p className="text-center sm:text-left">
              © 2024 CustoCare AI Health Platform. All rights reserved. 
              <span className="block sm:inline"> HIPAA-compliant healthcare data protection. For authorized healthcare use only.</span>
            </p>
            <div className="flex items-center gap-6">
              <motion.a
                whileHover={{ scale: 1.05 }}
                href="#"
                className="font-semibold text-blue-600 dark:text-blue-400 hover:underline"
                aria-label="Accessibility Statement"
              >
                Accessibility
              </motion.a>
              <div className="flex items-center gap-4">
                {['SOC 2', 'GDPR', 'CCPA'].map((cert) => (
                  <span
                    key={cert}
                    className={cn(
                      "px-3 py-1.5 text-xs rounded-full font-bold uppercase tracking-wider",
                      theme === 'dark' 
                        ? "bg-gray-800/60 text-gray-400" 
                        : "bg-gray-100 text-gray-600"
                    )}
                  >
                    {cert}
                  </span>
                ))}
              </div>
            </div>
          </div>
        </div>
      </motion.footer>

      {/* Premium Loading Overlay */}
      <AnimatePresence>
        {isLoading && (
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            role="dialog"
            aria-modal="true"
            aria-label="Loading Healthcare Platform"
            className="fixed inset-0 bg-black/60 backdrop-blur-xl flex items-center justify-center z-[9999]"
          >
            <motion.div
              initial={{ scale: 0.9, opacity: 0, y: 20 }}
              animate={{ scale: 1, opacity: 1, y: 0 }}
              exit={{ scale: 0.9, opacity: 0, y: 20 }}
              transition={{ type: "spring", damping: 25, stiffness: 200 }}
              className={cn(
                "p-12 rounded-3xl flex flex-col items-center gap-8 border shadow-2xl max-w-md mx-4",
                theme === 'dark' 
                  ? "bg-gray-900/90 border-gray-700" 
                  : "bg-white/95 border-gray-200"
              )}
            >
              {/* Animated Loading Circle */}
              <div className="relative">
                <motion.div
                  animate={{ rotate: 360 }}
                  transition={{ duration: 2, repeat: Infinity, ease: "linear" }}
                  className={cn(
                    "w-24 h-24 border-4 rounded-full",
                    theme === 'dark' 
                      ? "border-blue-500/20 border-t-blue-500" 
                      : "border-blue-500/20 border-t-blue-600"
                  )}
                />
                <motion.div
                  animate={{ scale: [1, 1.2, 1] }}
                  transition={{ duration: 1.5, repeat: Infinity }}
                  className="absolute inset-0 flex items-center justify-center"
                >
                  <Heart className="w-12 h-12 text-emerald-500" aria-hidden="true" />
                </motion.div>
              </div>
              
              {/* Loading Text */}
              <div className="text-center space-y-4">
                <motion.h3 
                  className={cn(
                    "text-2xl font-bold",
                    theme === 'dark' ? "text-white" : "text-gray-900",
                    Typography.h3
                  )}
                  animate={{ opacity: [1, 0.7, 1] }}
                  transition={{ duration: 1.5, repeat: Infinity }}
                >
                  {isLoading === 'login' 
                    ? 'Accessing Secure Dashboard' 
                    : 'Initiating Healthcare Journey'}
                </motion.h3>
                
                <div className="space-y-2">
                  <p className={cn(
                    "text-sm",
                    theme === 'dark' ? "text-gray-400" : "text-gray-600"
                  )}>
                    {isLoading === 'login' 
                      ? 'Verifying credentials & establishing secure connection...' 
                      : 'Configuring personalized healthcare environment...'}
                  </p>
                  
                  {/* Progress Indicators */}
                  <div className="flex items-center justify-center gap-2 pt-4">
                    {[1, 2, 3].map((i) => (
                      <motion.div
                        key={i}
                        animate={{ scale: [1, 1.3, 1] }}
                        transition={{ 
                          duration: 0.6, 
                          repeat: Infinity, 
                          delay: i * 0.2 
                        }}
                        className={cn(
                          "w-2 h-2 rounded-full",
                          theme === 'dark' ? "bg-blue-500" : "bg-blue-600"
                        )}
                      />
                    ))}
                  </div>
                </div>
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
};

/* ==========================================================================
   PREMIUM ACCESS PANEL COMPONENT
   ========================================================================== */
interface AccessPanelProps {
  theme: string;
  isLoading: string | null;
  hoveredOption: string | null;
  setHoveredOption: (option: string | null) => void;
  handleAction: (action: 'login' | 'signup') => Promise<void>;
  isMobile: boolean;
}

const AccessPanel: React.FC<AccessPanelProps> = ({
  theme,
  isLoading,
  hoveredOption,
  setHoveredOption,
  handleAction,
  isMobile
}) => {
  return (
    <motion.div
      initial={{ opacity: 0, scale: 0.95, y: 20 }}
      animate={{ opacity: 1, scale: 1, y: 0 }}
      transition={{ duration: 0.6, type: "spring" }}
      className="relative"
      role="complementary"
      aria-label="Account Access Panel"
    >
      {/* Floating AI Visualization Element */}
      <motion.div
        variants={floatAnimation}
        animate="animate"
        className={cn(
          "absolute -top-8 -right-8 w-36 h-36 rounded-2xl overflow-hidden border-4 shadow-3xl z-10",
          theme === 'dark' 
            ? "border-gray-800/80 shadow-blue-500/20" 
            : "border-white/80 shadow-blue-500/30"
        )}
        style={{ display: isMobile ? 'none' : 'block' }}
        role="img"
        aria-label="AI Healthcare Technology Visualization"
      >
        <img
          src={IMAGES.aiHealthcare}
          alt="AI Healthcare Technology"
          className="w-full h-full object-cover"
          loading="lazy"
        />
        <div className="absolute inset-0 bg-gradient-to-t from-blue-600/20 via-transparent to-emerald-600/10" />
        <motion.div
          animate={{ opacity: [0.3, 0.6, 0.3] }}
          transition={{ duration: 3, repeat: Infinity }}
          className="absolute inset-0 bg-gradient-to-tr from-blue-500/30 to-emerald-500/30"
        />
      </motion.div>

      {/* Main Card */}
      <motion.div
        whileHover={{ y: -4 }}
        className={cn(
          "rounded-3xl p-8 lg:p-10 backdrop-blur-2xl border shadow-3xl",
          theme === 'dark'
            ? "bg-gradient-to-br from-gray-800/80 to-gray-900/80 border-gray-700/60"
            : "bg-gradient-to-br from-white/95 to-blue-50/50 border-gray-200/60"
        )}
      >
        {/* Premium Header */}
        <motion.div 
          initial={{ opacity: 0, y: 10 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.2 }}
          className="text-center mb-10"
        >
          <motion.div 
            whileHover={{ scale: 1.1, rotate: 360 }}
            transition={{ duration: 0.6, type: "spring" }}
            className="relative inline-flex items-center justify-center w-20 h-20 rounded-3xl mb-6 border"
            style={{
              background: theme === 'dark'
                ? 'linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(16, 185, 129, 0.1))'
                : 'linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(16, 185, 129, 0.1))'
            }}
          >
            <Heart className="w-10 h-10 text-blue-600 dark:text-blue-400" aria-hidden="true" />
            <motion.div
              animate={{ scale: [1, 1.2, 1] }}
              transition={{ duration: 2, repeat: Infinity }}
              className="absolute inset-0 rounded-3xl border-2 border-blue-500/30"
            />
          </motion.div>
          
          <h2 className={cn(
            "text-3xl font-bold mb-4",
            Typography.h2,
            theme === 'dark' ? "text-white" : "text-gray-900"
          )}>
            Welcome to CustoCare AI
          </h2>
          <p className={cn(
            "text-base",
            theme === 'dark' ? "text-gray-400" : "text-gray-600",
            Typography.body
          )}>
            Secure access to intelligent healthcare management
          </p>
        </motion.div>

        {/* Premium Access Options */}
        <div className="space-y-6">
          {/* Create Account - Primary CTA */}
          <motion.button
            onClick={() => handleAction('signup')}
            disabled={isLoading !== null}
            onMouseEnter={() => setHoveredOption('signup')}
            onMouseLeave={() => setHoveredOption(null)}
            whileHover={{ scale: 1.02, y: -2 }}
            whileTap={{ scale: 0.98 }}
            className={cn(
              "w-full flex items-center justify-between p-6 lg:p-7 rounded-2xl transition-all duration-300 group relative overflow-hidden disabled:opacity-70 disabled:cursor-not-allowed",
              A11y.focusStyles,
              "shadow-xl hover:shadow-2xl",
              theme === 'dark'
                ? "bg-gradient-to-r from-blue-600 via-blue-500 to-emerald-600 hover:from-blue-700 hover:to-emerald-700"
                : "bg-gradient-to-r from-blue-600 via-blue-500 to-emerald-600 hover:from-blue-700 hover:to-emerald-700"
            )}
            aria-label="Create new account - Begin your healthcare journey"
          >
            {/* Animated gradient overlay */}
            <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
            
            {/* Animated shimmer effect */}
            <motion.div
              className="absolute inset-0"
              animate={hoveredOption === 'signup' ? { x: ['-100%', '100%'] } : {}}
              transition={{ duration: 1.5, repeat: Infinity }}
              style={{
                background: 'linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent)',
              }}
            />
            
            <div className="flex items-center gap-5 relative z-10">
              <motion.div 
                whileHover={{ rotate: 360, scale: 1.1 }}
                transition={{ duration: 0.6 }}
                className="w-16 h-16 rounded-2xl bg-white/20 backdrop-blur-sm flex items-center justify-center shrink-0 border border-white/30"
              >
                <UserCheck className="w-8 h-8 text-white" aria-hidden="true" />
              </motion.div>
              <div className="text-left">
                <h3 className="text-xl font-bold text-white mb-2">
                  Begin Your Journey
                </h3>
                <p className="text-sm text-white/90 leading-relaxed max-w-xs">
                  Register as Patient, Medical Professional, or Healthcare Facility.
                  AI-powered onboarding included.
                </p>
              </div>
            </div>
            <motion.div
              animate={hoveredOption === 'signup' ? { x: 6 } : { x: 0 }}
              transition={{ type: "spring", stiffness: 400 }}
              className="relative z-10"
            >
              <ArrowRight className="w-7 h-7 text-white shrink-0" aria-hidden="true" />
            </motion.div>
          </motion.button>

          {/* Access Existing Account - Secondary */}
          <motion.button
            onClick={() => handleAction('login')}
            disabled={isLoading !== null}
            onMouseEnter={() => setHoveredOption('login')}
            onMouseLeave={() => setHoveredOption(null)}
            whileHover={{ scale: 1.02, y: -2 }}
            whileTap={{ scale: 0.98 }}
            className={cn(
              "w-full flex items-center justify-between p-6 lg:p-7 rounded-2xl border-2 transition-all duration-300 group disabled:opacity-70 disabled:cursor-not-allowed",
              A11y.focusStyles,
              "hover:shadow-xl backdrop-blur-sm",
              theme === 'dark'
                ? "bg-gray-800/50 border-gray-700/50 hover:bg-gray-800/80 hover:border-blue-500/50"
                : "bg-white/80 border-gray-300/80 hover:bg-white hover:border-blue-500/50"
            )}
            aria-label="Login to existing account - Access your healthcare dashboard"
          >
            <div className="flex items-center gap-5">
              <motion.div 
                whileHover={{ rotate: 360, scale: 1.1 }}
                transition={{ duration: 0.6 }}
                className={cn(
                  "w-16 h-16 rounded-2xl flex items-center justify-center shrink-0 border",
                  theme === 'dark' 
                    ? "bg-blue-500/20 border-blue-500/30" 
                    : "bg-blue-100 border-blue-200"
                )}
              >
                <Shield className={cn(
                  "w-8 h-8",
                  theme === 'dark' ? "text-blue-400" : "text-blue-600"
                )} aria-hidden="true" />
              </motion.div>
              <div className="text-left">
                <h3 className={cn(
                  "text-xl font-bold mb-2",
                  theme === 'dark' ? "text-white" : "text-gray-900",
                  Typography.h3
                )}>
                  Access Your Account
                </h3>
                <p className={cn(
                  "text-sm leading-relaxed max-w-xs",
                  theme === 'dark' ? "text-gray-400" : "text-gray-600"
                )}>
                  Continue to your secure healthcare dashboard with enterprise-grade authentication.
                </p>
              </div>
            </div>
            <motion.div
              animate={hoveredOption === 'login' ? { x: 6 } : { x: 0 }}
              transition={{ type: "spring", stiffness: 400 }}
            >
              <ChevronRight className={cn(
                "w-7 h-7 shrink-0",
                theme === 'dark' ? "text-gray-400" : "text-gray-500"
              )} aria-hidden="true" />
            </motion.div>
          </motion.button>
        </div>

        {/* Enterprise Security Assurance */}
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.6 }}
          className={cn(
            "mt-10 pt-8 border-t",
            theme === 'dark' ? "border-gray-700/50" : "border-gray-200/50"
          )}
          role="region"
          aria-label="Security Information"
        >
          <div className="flex items-center justify-center gap-3 mb-4">
            <motion.div
              animate={{ scale: [1, 1.2, 1] }}
              transition={{ duration: 2, repeat: Infinity }}
            >
              <Lock className="w-5 h-5 text-emerald-500" aria-hidden="true" />
            </motion.div>
            <span className={cn(
              "text-sm font-bold tracking-wider uppercase",
              theme === 'dark' ? "text-gray-300" : "text-gray-700",
              Typography.caption
            )}>
              Enterprise-Grade Security
            </span>
          </div>
          
          <div className="grid grid-cols-3 gap-3 mb-6">
            {[
              { label: "HIPAA", color: "emerald" },
              { label: "SOC 2", color: "blue" },
              { label: "GDPR", color: "purple" }
            ].map((badge) => (
              <div
                key={badge.label}
                className={cn(
                  "py-2.5 rounded-lg text-center text-xs font-bold uppercase tracking-wider",
                  badge.color === 'emerald' && "bg-emerald-500/10 text-emerald-700 dark:text-emerald-300",
                  badge.color === 'blue' && "bg-blue-500/10 text-blue-700 dark:text-blue-300",
                  badge.color === 'purple' && "bg-purple-500/10 text-purple-700 dark:text-purple-300"
                )}
              >
                {badge.label}
              </div>
            ))}
          </div>
          
          <p className={cn(
            "text-center text-sm leading-relaxed max-w-sm mx-auto",
            theme === 'dark' ? "text-gray-500" : "text-gray-600"
          )}>
            Your healthcare data is protected with 256-bit encryption, HIPAA compliance, 
            and ISO 27001 certification. Zero-knowledge architecture ensures privacy.
          </p>
        </motion.div>
      </motion.div>
    </motion.div>
  );
};

export default Landing;